// This component is no longer in use. 
// Its functionality has been replaced by the ChatMessage component in App.tsx,
// which supports markdown rendering, streaming responses, and better styling.
import React from 'react';

const DeprecatedMessage: React.FC = () => {
    return (
        <div>
            <p>This component has been replaced.</p>
        </div>
    );
};

export default DeprecatedMessage;
